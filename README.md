# Web-Development-Interns-Task-4

# Task 6: Contact Form with JavaScript Validation

## 📘 Objective
Build a **Contact Form** with **client-side validation** using HTML, CSS, and JavaScript.  
The goal of this task is to learn how to handle user inputs, perform input validation, and provide real-time feedback to users.

---

## 🛠️ Tools Used
- **Visual Studio Code (VS Code)**
- **Google Chrome Browser**
- **HTML5**
- **CSS3**
- **JavaScript (ES6)**

---

## 🧩 Features
✅ Input fields for **Name**, **Email**, and **Message**  
✅ Validation to ensure:
- All fields are **non-empty**
- Email follows a **valid format** using regex  
✅ Shows **error messages** below inputs when validation fails  
✅ Prevents form submission when inputs are invalid  
✅ Displays **success message** when all inputs are valid  
✅ Clean and responsive UI using simple CSS  

---

## 📁 Project Structure

---

## 🧠 Learning Outcomes
- Form handling in **HTML**
- **Client-side validation** using JavaScript
- Using **regular expressions (regex)** for email validation
- Providing **real-time feedback** to users
- Preventing form submission with `event.preventDefault()`

---

## 🚀 How to Run the Project
1. Clone this repository:
   ```bash
   git clone https://github.com/your-username/Web-Development-Interns-Task-4.git
